pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Build stage is running'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploy stage is running'
            }
        }
    }
}
